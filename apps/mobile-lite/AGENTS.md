# Solviax Lite

Part of the Solviax.app monorepo. Follow root `/AGENTS.md` and `.cursor/rules/dual-platform-i18n.mdc`.

- Shell copy (splash / offline) lives in `packages/i18n` (`mobileLite.*`) — EN + FI together.
- Product UI changes belong in **`apps/web`** (this app only hosts them in a WebView).
- Store / billing rules: **[STORE.md](./STORE.md)**.
- Expo SDK docs: https://docs.expo.dev/versions/v57.0.0/
