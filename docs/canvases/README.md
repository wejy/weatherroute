# Canvases (archived copies)

Live Cursor Canvases are edited under:

`~/.cursor/projects/home-wejuk-projects-weatherapp/canvases/`

That path is **outside** this git repo. Copies here are for version control and
long-term reference. Open the `.cursor/.../canvases/*.canvas.tsx` file in the
IDE to use the interactive Canvas UI.

| File | Topic |
|---|---|
| `pricing-monetization.canvas.tsx` | Product pricing, API costs, monetization models |
| `security-audit.canvas.tsx` | Production security assessment |
| `db-architecture-review.canvas.tsx` | Database architecture review |
| `wcag-audit.canvas.tsx` | Accessibility (WCAG) audit |

When you update a live canvas, re-copy it here (or ask the agent to sync) so
git keeps the latest.
