# @solviax/i18n

Shared English + Finnish copy for Solviax web and Expo.

## Rules

- Every string key must exist in **both** `src/en.ts` and `src/fi.ts`.
- Apps import via `@solviax/i18n` — do not fork dictionaries per app.
