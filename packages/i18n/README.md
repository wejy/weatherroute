# @weathertrip/i18n

Shared English + Finnish copy for WeatherTrip web and Expo.

## Rules

- Every string key must exist in **both** `src/en.ts` and `src/fi.ts`.
- Apps import via `@weathertrip/i18n` — do not fork dictionaries per app.
